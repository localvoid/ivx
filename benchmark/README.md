# ivx Benchmarks

Install all dependencies.

```sh
$ yarn
```

Compile from TypeScript.

```sh
$ npm run dist
```

Launch HTTP server.

```sh
$ npm run serve
```

Run web benchmarks.

```sh
$ ./benchmark.sh
```

Run basic benchmarks.
```sh
$ node ./dist/benchmark.js
```